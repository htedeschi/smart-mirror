function ready() {
    // set link
    var link = document.getElementById('activate-link');
    link.innerText = window.location.host + '/activate';

    // get a new mirror code

    // display mirror code

    // check if the new mirror was added
    // if so, load the confs and redirect to /mirror
}